<%= form_for [current_user, @booking] do |f| %>

  <%= f.label "Description" %>
  <%= f.text_area :description %>

  <%= f.collection_select :flight, Flight.all, :id, :name %>



  <%= f.submit %>
<% end %>
