<%= form_for @booking do |f| %>

  <%= f.label "Description" %>
  <%= f.text_area :description %>
  <%= f.collection_select :flight_id, Flight.all, :id, :name %>
  <%= f.collection_select :user_id, User.all, :id, :name %>
  <%= f.submit %>
<% end %>
