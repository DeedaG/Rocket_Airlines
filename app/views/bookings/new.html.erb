  <%= form_for @booking do |f| %>

    <%= f.label "Notes" %>
    <%= f.text_area :description %>

    <%= f.label "Paid?"%>
    <%= f.check_box :paid %>

    <%= f.hidden_field :flight, value: params['flight_id'] %>

    <%= f.submit %>
  <% end %>
